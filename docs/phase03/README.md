# Phase 3: Positional Embeddings

**Status:** Not started yet.

After Phase 2 (token embeddings), Phase 3 will cover:

- Why transformers need position information
- Learned positional embeddings
- `token_embedding + position_embedding` → shape `(B, T, C)`

Docs will be added here when you say **go** for Phase 3.
